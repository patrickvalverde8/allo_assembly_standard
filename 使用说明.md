# 使用说明

## 快速开始

### 1. 查看可用装配
```bash
python build.py
```

### 2. 构建装配
```bash
# 简单框架
python build.py assemblies/02_box_lamp.json

# 复杂错位灯具（推荐）
python build.py assemblies/03_complex_lamp.json
```

### 3. 查看结果
在 CAD 软件中打开 `output/` 目录下的 STEP 文件。

## 装配文件

所有装配定义在 `assemblies/` 目录：

- **01_nodes_test.json** - 节点测试（横排）
- **02_box_lamp.json** - 机箱风格灯具（简单框架）
- **03_complex_lamp.json** - 复杂错位灯具（推荐）

## 创建新装配

### 1. 创建 JSON 文件
在 `assemblies/` 目录创建新文件，例如 `03_my_lamp.json`

### 2. 定义结构
```json
{
  "name": "我的灯具",
  "unit_mm": 1.0,
  "nodes": [...],
  "rods": [...],
  "panels": [],
  "rod_attachments": []
}
```

### 3. 构建
```bash
python build.py assemblies/03_my_lamp.json
```

## 节点端口

### 十字连接件 (node_cross_6x6)
- `x_pos`, `x_neg` - 左右
- `y_pos`, `y_neg` - 前后
- `z_pos`, `z_neg` - 上下

### T型连接件 (node_T_6x6)
- `y_pos`, `y_neg` - 前后
- `x_neg` - 左

### 平行连接件 (node_parallel_6x6_c10)
- `right_axis` - 右孔
- `left_axis` - 左孔

### 支撑座/固定环
- `rod_axis` - 光轴孔

## 坐标系

```
     +Z (上)
      |
      |
      +------ +X (右)
     /
    /
  +Y (前)
```

## 输出

- 位置：`output/` 目录
- 格式：`YYYYMMDD_HHMMSS_装配名.step`
- 示例：`20251118_141145_02_box_lamp.step`

## 文件结构

```
├── nodes/               # 节点定义和 STEP 文件
├── assemblies/          # 装配定义
├── output/              # 输出文件
└── build.py             # 主程序
```
